import { HeightWeightPipe } from './height-weight.pipe';

describe('HeightWeightPipe', () => {
  it('create an instance', () => {
    const pipe = new HeightWeightPipe();
    expect(pipe).toBeTruthy();
  });
});
