import { DefaultpokemonImagePipe } from './defaultpokemon-image.pipe';

describe('DefaultpokemonImagePipe', () => {
  it('create an instance', () => {
    const pipe = new DefaultpokemonImagePipe();
    expect(pipe).toBeTruthy();
  });
});
